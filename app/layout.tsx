import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Strategy Reality Check - Is Your Trading Strategy Real or Just Luck?',
  description: 'Upload your closed trades and instantly see if your edge is statistically valid. Free, fast, and no setup required.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
